# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=docs_db

# Backend Configuration
BACKEND_PORT=3001
CORS_ORIGIN=*

# Frontend Configuration  
FRONTEND_PORT=3000

# API URL for Frontend (set this to your backend's external URL)
# Leave empty to use same-origin (if using reverse proxy)
# Or set to full URL like: https://api.yourdomain.com
VITE_API_URL=
